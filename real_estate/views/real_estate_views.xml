<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- List View -->
    <record id="real_estate_view_list" model="ir.ui.view">
        <field name="name">real_estate.view.list</field>
        <field name="model">real.estate</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="state"/>
                <field name="amount"/>
                <field name="date"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="real_estate_view_form" model="ir.ui.view">
        <field name="name">real_estate.view.form</field>
        <field name="model">real.estate</field>
        <field name="arch" type="xml">
            <form string="Form">
                <header>
                    <button name="action_do_something" type="object" string="Do Something" class="oe_highlight"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="partner_id"/>
                            <field name="amount"/>
                            <field name="date"/>
                        </group>
                        <group>
                            <field name="company_id"/>
                            <field name="description"/>
                        </group>

                        <field name="active" invisible="1"/>
                    </group>
                </sheet>
                 <chatter/>
            </form>

        </field>
    </record>

    <!-- Kanban View -->
    <record id="real_estate_view_kanban" model="ir.ui.view">
        <field name="name">real_estate.view.kanban</field>
        <field name="model">real.estate</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="name"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_global_click">
                            <strong><field name="name"/></strong><br/>
                            <field name="state"/>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Calendar View -->
    <record id="real_estate_view_calendar" model="ir.ui.view">
        <field name="name">real_estate.view.calendar</field>
        <field name="model">real.estate</field>
        <field name="arch" type="xml">
            <calendar string="Calendar" date_start="date">
                <field name="name"/>
            </calendar>
        </field>
    </record>

    <!-- Pivot View -->
    <record id="real_estate_view_pivot" model="ir.ui.view">
        <field name="name">real_estate.view.pivot</field>
        <field name="model">real.estate</field>
        <field name="arch" type="xml">
            <pivot>
                <field name="state"/>
                <field name="amount"/>
            </pivot>
        </field>
    </record>

    <!-- Search View -->
    <record id="real_estate_view_search" model="ir.ui.view">
        <field name="name">real_estate.view.search</field>
        <field name="model">real.estate</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="state"/>
                <filter string="Draft" name="state" domain="[('state','=','draft')]"/>
                <filter string="Active" name="active" domain="[('active','=',True)]"/>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_real_estate" model="ir.actions.act_window">
        <field name="name">Real Estate</field>
        <field name="res_model">real.estate</field>
        <field name="view_mode">list,form,kanban,calendar,pivot</field>
    </record>

    <!-- Client Action -->
    <record id="real_estate_action" model="ir.actions.client">
            <field name="name">Client Action</field>
            <field name="tag">real_estate.real_estate</field>
    </record>
    <menuitem id="menu_real_estate_root" name="real_estate" action="real_estate_action"/>

    <!-- Menu -->
    <menuitem id="menu_real_estate_root" name="Real Estate"/>
    <menuitem id="menu_real_estate_main" name="Records" parent="menu_real_estate_root" action="action_real_estate"/>
</odoo>
