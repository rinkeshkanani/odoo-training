<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- List View -->
    <record id="event_management_view_list" model="ir.ui.view">
        <field name="name">event_management.view.list</field>
        <field name="model">event.management</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="state"/>
                <field name="amount"/>
                <field name="date"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="event_management_view_form" model="ir.ui.view">
        <field name="name">event_management.view.form</field>
        <field name="model">event.management</field>
        <field name="arch" type="xml">
            <form string="Form">
                <header>
                    <button name="action_do_something" type="object" string="Do Something" class="oe_highlight"/>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="state"/>
                        <field name="description"/>
                        <field name="amount"/>
                        <field name="date"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Kanban View -->
    <record id="event_management_view_kanban" model="ir.ui.view">
        <field name="name">event_management.view.kanban</field>
        <field name="model">event.management</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="name"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_global_click">
                            <strong><field name="name"/></strong><br/>
                            <field name="state"/>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Calendar View -->
    <record id="event_management_view_calendar" model="ir.ui.view">
        <field name="name">event_management.view.calendar</field>
        <field name="model">event.management</field>
        <field name="arch" type="xml">
            <calendar string="Calendar" date_start="date">
                <field name="name"/>
            </calendar>
        </field>
    </record>

    <!-- Pivot View -->
    <record id="event_management_view_pivot" model="ir.ui.view">
        <field name="name">event_management.view.pivot</field>
        <field name="model">event.management</field>
        <field name="arch" type="xml">
            <pivot>
                <field name="state"/>
                <field name="amount"/>
            </pivot>
        </field>
    </record>

    <!-- Search View -->
    <record id="event_management_view_search" model="ir.ui.view">
        <field name="name">event_management.view.search</field>
        <field name="model">event.management</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="state"/>
                <filter string="Draft" name="state" domain="[('state','=','draft')]"/>
                <filter string="Active" name="active" domain="[('active','=',True)]"/>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_event_management" model="ir.actions.act_window">
        <field name="name">Event Management</field>
        <field name="res_model">event.management</field>
        <field name="view_mode">list,form,kanban,calendar,pivot</field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_event_management_root" name="Event Management"/>
    <menuitem id="menu_event_management_main" name="Records" parent="menu_event_management_root" action="action_event_management"/>
</odoo>
